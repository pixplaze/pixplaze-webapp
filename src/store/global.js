import {defineStore} from 'pinia'

export const useGlobalStore = defineStore('global', {
  state: () => ({
    loading: true,
    asideExpanded: false
  }),
  getters: {
    
  },
  actions: {
    
  }
})